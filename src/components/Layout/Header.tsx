import { Box } from '@chakra-ui/react';
import React from 'react';

const Header: React.FC = () => {
   return (
      <Box position={'fixed'} bg={'#000'} w={'full'} h={'50px'}>
         Header
      </Box>
   );
};

export default Header;
